<% layout('./layouts/boilerplate') %>
    <div class="row">
        <div class="col-md-2">
            <a href="/campgrounds" class="btn btn-outline-secondary">
                <<< </a>
        </div>
        <div class="col-md-6">
            <h1 class="display-6">
                <%=campground.title%>
            </h1>
            <h2>
                <%=campground.location%>
            </h2>
        </div>
        <div class="col-md-4">
            <a href="/campgrounds/<%= campground._id %>/edit"
                class="btn btn-outline-warning me-3">Edit</a>
            <form action="/campgrounds/<%= campground._id %>?_method=DELETE"
                method="POST" style="display: inline-block;">
                <p><button class="btn btn-outline-danger">Delete</button> </p>
            </form>
        </div>
    </div>

    <img src="<%= campground.image %>" alt=""
        class="img-fluid border rounded shadow mb-5 bg-body rounded">
    <p class="lead">
        <%= campground.description %>
    </p>